using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ComponentFun : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameObject.Find("Spiderman").SetActive(true);
       //ameObject.SetActive(false); //This would turn off the gameObject in the scene.
       //ameObject.SetActive(true); //This would turn on the gameObject in the scne.
       //ameObject.transform.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
